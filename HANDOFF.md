# 看護薬理ノート — Claude Code 引き継ぎ仕様書

このドキュメントは、claude.ai（チャット）側での設計検討をまとめ、Claude Code側で
実装を進めるための引き継ぎ資料です。役割分担は以下の通りです。

- **claude.ai（このチャット）**：データ設計・パース処理・相互作用ロジック・仕様決定
- **Claude Code**：GitHubリポジトリ作成、フロントエンド実装（`frontend-design`スキル使用）、
  GitHub Actions構築、Netlifyデプロイ設定
- **GPT**：タイトル画面等のビジュアル素材生成（Claudeの画像生成は使わない方針）

## 1. プロジェクト概要

- 名称：看護薬理ノート（確定。ドメインは未決定）
- 対象ユーザー・表示レベルの対応（2026-09-11訂正）：
  - **かんたん表示（デフォルト）**：一般の方、在宅で家族の介護・看病を行う人、介護職員。
    専門知識を前提としない層
  - **くわしく表示**：看護学生を含む医療従事者。添付文書に近い専門的な記述でよい層
  - 「看護で見る」等の項目名・専門的な視点による情報整理は情報の質を担保するための設計であり、
    かんたん表示の対象者（一般人・介護職員含む）にも、分かりやすい言葉で内容自体は届ける
    （項目を非表示にする・対象者を絞るという意味ではない）
- 対象薬剤範囲：診療科・年齢層を限定しない、全薬剤（新生児から終末期まで、
  産婦人科・小児科・精神科・緩和ケア等すべての領域を含む）
- 位置づけ：完全無料の単独公開ツール（KarteNoへの導線としては使わない）
- 開発方針：まずWEB版を作り始める。必要な機能はその都度追加していく（一括で全部作り切ろうとしない）
- アプリ化・販売：保留（否定ではなく先送り）。当面の設計・実装には影響させない
- ホスティング：静的サイト、Netlify（福盛堂と同じ運用パターン）
- リポジトリ：新規GitHubリポジトリ（Claude Codeで作成）

## 2. データパイプライン（実装済み・添付ファイル参照）

- `pmda_tenpu_parser.py`：PMDA添付文書XML（新記載要領）を読み込み、SQLiteへクレンジング
- `drug_alias.py`：薬効分類名⇄一般名の対応表と、相互作用テキストの自動突合ロジック
- 出力：`drug_master`（薬品基本情報＋薬効分類）、`drug_interaction`（各薬の相互作用記載）、
  `drug_pair_interaction`（自動突合済みの薬剤ペア）の3テーブルを持つSQLite

### データソースと更新方針

- 初回：PMDA「医療用医薬品 添付文書等情報検索」から添付文書XML（新記載要領）を取得
- 更新：PMDAの検索ページには「更新年月日で検索」条件があるため、直近改訂分のみを検知して
  差分取得する設計にする（全件再取得はしない）
- **未解決・要検証**：PMDAは公式ダウンロードAPIを提供しておらず、検索ページの挙動に依存した
  スクレイピングになる。実際のフォームパラメータ・レスポンス構造はClaude Code側で
  実機検証（実際にリクエストを送って確認）が必要。ここは私（claude.ai）側では
  ネットワークアクセスの制約上、検証できていない
- 更新頻度：月次のGitHub Actions（無料枠）。自動ジョブが失敗した場合は
  GitHub Actionsの標準の失敗通知（リポジトリ設定でメール等）が飛ぶようにしておくこと。
  「気づかず何ヶ月も放置」を避けるため、失敗通知は必須

## 3. 静的サイトへのデータ供給

SQLiteは動いているサーバーではないため、静的サイトはSQLiteに直接問い合わせできない。
ビルド時に以下の変換を挟むこと。

1. `pmda_tenpu_parser.py`でSQLiteを更新
2. SQLite → JSON書き出し（軽量な検索用インデックスへ変換。全薬剤分を1ファイルに
   まとめると重くなる可能性が高いので、カテゴリ別・頭文字別等の分割を検討すること）
3. JSONをサイトに同梱してNetlifyへデプロイ

この3ステップをGitHub Actions 1本のワークフローにまとめ、「更新チェック→差分DL→
パース→DB更新→JSON書き出し→Netlifyデプロイ」を自動化する。

## 4. UI仕様（claude.aiでプロトタイプ検証済み）

### 個別薬品ページ

以下の情報を持つカード形式（プロトタイプはこのチャット内のウィジェットで確認済み。
見た目はClaude Codeで`frontend-design`スキルを使い作り直すこと。プロトタイプの
見た目をそのまま採用する前提ではない）：

- 薬品名・一般名・薬効分類
- 効能・効果
- 用法・用量
- 重要な注意（重大な副作用等）
- 飲み合わせ注意（相互作用）
- 施設ごとの注意点（第2層。初期状態は「未登録」、施設の院内処方リストが
  取り込まれた薬剤のみ表示される）

表示は「かんたん表示（デフォルト）」「くわしく表示（添付文書に近い専門的文言）」の
2段階切り替えとする（画面上のラベル名。旧称「平易表示／詳細表示」から2026-09-11に変更）。
対象ユーザーと表示レベルの対応は1章参照。デフォルトは専門知識を前提としない言葉にすること。

### 複数薬チェック機能

- 薬品名で検索し、服用中の薬にチェックを入れていく
- 2つ以上チェックすると、`drug_pair_interaction`テーブルを参照し、該当する組み合わせに
  警告を表示
- 相互作用が見つからない組み合わせは「記載されている相互作用は見つかりませんでした」と
  正直に表示する（「安全」と誤解されないように）
- 各薬の効能・効果が重なる場合、「共通の適応」として参考表示できる。
  ただし**これは診断ではない**という文言を必ず添える

## 5. 第2層（施設ごとの注意点）の設計

- 施設ごとに採用薬品・注意点は異なるため、第2層は「施設単位」で管理する
- 初期データ範囲は藤丸の勤務先介護医療院の院内処方リスト（撮影後にテキスト化して
  `formulary.txt`として`pmda_tenpu_parser.py`に渡す運用。まだ未提供）
- 薬理辞典WEB側は現状「施設ごとの注意点」を表示する場所は用意するが、
  施設単位でログインを分けるような複雑な機能は現時点でスコープ外
  （KarteNo側で利用者の処方データと直結させる使い方の方が本命）

## 5.1 KarteNo連携要件（2026-09-10追加）

KarteNoとは接続する方針（データパイプラインは共有、機能は別）。KarteNo側では
以下2つを出すこと。

- **アラート**：既存設計通り。利用者の定時薬×臨時薬・観血的処置を`drug_pair_interaction`
  （タグ突合含む）で照合し警告表示
- **観察項目**：アラートが出た薬について、「何を観察すべきか」をあわせて表示する。
  例：ジゴキシン→徐脈・悪心嘔吐・食欲不振の有無を観察、フロセミド→脱水兆候・
  血清カリウム値を確認

**未解決の設計課題**：現状のスキーマ（`drug_master`）には「観察項目」を独立して
持つ列が無い。`serious_adverse_events`（重大な副作用）と、相互作用テーブルの
`clinical_symptom_action`（臨床症状・措置方法）にヒントとなる文言は入っているが、
「観察ポイントの箇条書き」として構造化はされていない。対応方針の選択肢：

1. v1は`serious_adverse_events`と`clinical_symptom_action`のテキストをそのまま
   「観察項目」欄に流用する（自動・粗いが早く出せる）
2. 観察項目専用の列を`drug_master`に追加し、看護知識で個別に加筆する
   （精度は高いが第2層と同様、手作業が発生し全薬剤には拡張しにくい）

まずは1で動くものを作り、精度が問題になった薬剤から2へ移行するのが現実的
（この判断はClaude Code着手後、実データを見ながら確定でよい）。

## 6. 法務・安全面（公開前に必須）

- **免責事項**：診断・治療の代替ではないこと、最新の添付文書を必ず確認することを明記
- **著作権表記**：PMDAのデータは「公共データ利用規約（第1.0版）」（PDL1.0）に基づく。
  出典明記と「編集・加工した」旨の記載が必要。「公共データ利用規約に関する重要情報」
  ページに例外規定がないか、公開前に確認すること（未確認のまま）
- 誤り報告の導線（フォーム等）は初期リリースでは無くても致命的ではないが、
  早期に追加すべき

## 7. まだ決まっていないこと

- ドメイン
- 全薬剤分のJSONを分割する際の具体的な分割単位
- PMDA差分検知の実装方法（上記「未解決・要検証」参照）
- 誤り報告フォームの要否・実装方法
- 観察項目データの持たせ方（上記5.1参照、v1は簡易対応でよい）
- `site/data/interactions/`配下の重複排除（現状は薬剤ペアをA視点・B視点で二重に
  持っており全件展開時418MBになる。`interactions/{min_id}_{max_id}.json`のような
  ペア単位1ファイルに統合し、二重持ちをやめるのは将来の最適化課題）

## 8. 【将来検討】PWA化・機能拡張の方向性（2026-09-11追加、課金境界線は当面なし）

- 将来的にPWA化（ホーム画面への追加）を検討する。現時点では未決定
- 機能は無料のまま追加していく方針（比較表示、検査値×薬、症状×薬、学習モード等）
- 安全性に関わる情報（相互作用・禁忌・重大な副作用）は、今後も無料のまま維持する方針
- 課金モデル自体は保留。仮に将来検討する場合も、安全情報を有料の壁の向こうに
  置かない設計にすること

## 添付ファイル

- `pmda_tenpu_parser.py`
- `drug_alias.py`
- `export_db_to_json.py`（DB→JSON書き出しの叩き台。分割方法は未実装、要調整）
- `.github-workflows-update-drug-db.yml`（GitHub Actionsワークフローの叩き台。
  `.github/workflows/update-drug-db.yml`としてリポジトリに配置すること。
  PMDA差分検知部分は未実装のプレースホルダー）
