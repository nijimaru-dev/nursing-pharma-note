# DESIGN.md — デザイン定義書 v1.0（統合版）

このファイルは、Notion正典「[看護薬理ノート 正典 v0.1](https://app.notion.com/p/3d7a4c2a1e9b813a86f7c7926e8abd0d)」末尾の
「デザイン定義書 v1.0（統合版）」と同内容を、リポジトリ側にも保持するためのものです。
新規UI設計・実装・レビューの基準は、このファイルを正とします（`CLAUDE.md`から参照）。

Notion側とこのファイルの内容が食い違った場合は、Notion側（正典）を正とし、このファイルを追随させてください。

---

## 1. 基本方針

2つの失敗パターンを両方避ける。

- **AIの量産感**：意味のない装飾色、テンプレ的な角丸・影・グラデーション・反語見出しの多用
- **政府系の無機質さ**：文字だけの画面、色を削りすぎて温かみがない、情報がどこにあるか分からない導線

色数を抑えるのは「意味なく色を使わない」ためであり、「視覚的な情報量・見やすさ・温かみ」を削るためではない。
アイコン・イラスト・画像は積極的に使う（絵文字は除く）。

## 2. カラー体系

| 用途 | 色 | 備考 |
|---|---|---|
| ブランドカラー | **確定：深い青緑（teal） `#0c5f57`**（ダークモードは`#5cc0b3`） | ボタン・リンク・選択状態のみに使用。装飾には使わない。単一のフラットカラーとし、グラデーションにはしない（`site/css/style.css`の`--teal`系トークンで運用中） |
| 背景 | 白 | |
| カード枠線 | 薄いグレー1色 | 装飾的な色付きボーダーは使わない |
| 本文テキスト | 濃いグレー〜黒 | コントラスト比4.5:1以上 |
| 禁忌バッジ | 赤系＋「禁忌」の文字ラベル＋専用アイコン | 色だけに頼らない（色覚特性への配慮） |
| 注意バッジ | 橙系＋「注意」の文字ラベル＋専用アイコン | 同上 |
| リンク | 既定＝青、訪問済み＝紫 | Web標準の慣習に合わせる |

## 3. タイポグラフィ

- 見出し：Shippori Mincho（明朝体）
- 本文：Zen Kaku Gothic New
- 游明朝体／游ゴシック・Noto Serif JP／Noto Sans JPはいずれもフォールバックとして
  明示的にフォントスタックへ含める（Webフォント未読込時もブラウザ標準フォントへ
  落ちないようにするため。`site/css/style.css`の`--font-display`/`--font-body`参照）
- 見出しと本文をあえて異なる書体（明朝×ゴシック）にすることで「専門書＋現代的Webアプリ」の
  質感を出す方針とし、実装済みの構成を正とする
- 見出し内で1単語だけを強調する処理（太字・斜体・色替え）は禁止

## 4. アイコン・絵文字方針

絵文字は禁止。意味のある箇所（錠剤・警告・飲み合わせ等）にSVG線画アイコンを単色で使用。色は機能色ルールに従う。

## 5. レイアウト

- PC表示時のコンテンツ最大幅をモバイル基準から拡張し、検索結果・カテゴリ一覧は複数カラムで表示する
  （現状はモバイル用の狭い幅をPCにもそのまま適用しており、中央に寄って間延びして見える問題を解消する）
- drug.html（薬剤詳細ページ）の情報を「常時表示（効能・用法用量・禁忌・飲み合わせ注意）」と
  「折りたたみ表示（副作用詳細・薬物動態・その他添付文書記載事項）」に分離し、アコーディオンUIで実装する
- トップの文字サイズ切り替え（小・中・大）と並べて「簡単表示／詳しく見る」の切り替えを新設し、
  簡単表示では折りたたみセクションを最初から非表示にする

## 6. コピーライティング／UXライティング

- 反語・煽り系のキャッチコピー禁止（「その〇〇、なぜ〜」等）。トップの見出しは現在の小見出し
  「看護師・介護職員・看護学生のための薬剤リファレンス」をメイン見出しとして扱う
- ボタン・ラベルは具体的な行動を表す言葉にする（例：「保存」ではなく「マイ薬箱に追加」）
- 中黒（・）でつないだメタ文字列、スペース付きダッシュの「WORD — fragment」ラベル、
  リンク末尾への機械的な「→」付与は使わない

## 7. 信頼性・アクセシビリティ表示（新規）

- 各薬剤ページに「このデータはPMDA添付文書の◯年◯月時点」という取込日時を明記する
- 禁忌・注意等の警告表示は色だけに頼らず、文字ラベル＋アイコンを必ず併記する（色覚特性への配慮）
- ボタン・チェックボックスのタップ領域は十分な大きさを確保し、多忙な業務中・片手操作でも押し間違えにくくする
- ダークモードは実装済み（夜勤時の使用に対応）、今後も維持する

## 8. AIが陥りがちな手抜きパターン（禁止リスト）

「絶対禁止」ではなく「選ぶ理由なく採用するのを禁止」という位置づけ。

- **色**：紫〜青グラデーション（"AI Blue"）、暖色クリーム背景×テラコッタ系アクセント（`#D97757`近辺）、
  黒背景×蛍光アクセント1色、全カード同一の薄灰色影（`rgba(0,0,0,.1)`）
- **フォント**：Inter/Roboto/Arial中心の指定、見出し内で1単語だけの強調、ラベルの全部大文字、
  意味のないeyebrowラベル
- **レイアウト**：全カード同一角丸の機械的適用、意味のない01/02/03番号マーカー（本当に手順・時系列でない限り）
- **モーション**：カードhoverでの一律translateY、スクロール時の一律フェードイン＋スライドアップ
- **細部**：中黒でつないだメタ文字列、スペース付きダッシュの「WORD — fragment」ラベル、
  純黒でない微妙な黒（`#0B0B0B`等）、リンク末尾への機械的な「→」付与

## 9. 運用ルール

- この定義はNotion正典とリポジトリの`DESIGN.md`（`CLAUDE.md`から参照）の両方に保持する。
  クロコ（Claude Code）の実装はこの`DESIGN.md`を都度参照すること
- 新規UI設計時はClaude Codeの`/design`スキル（複数案生成）を活用し、1案だけで確定しない
- ブランドカラーが決まり次第、本ドキュメントの2章を更新する
