# CLAUDE.md

このリポジトリで作業するときは、以下を必ず守ってください。

## データの唯一の原本

- `karteno_drugs.db`（`pmda_tenpu_parser.py`が生成するSQLite）が全データの原本。
- `site/data/`配下のJSON類は`export_db_to_json.py`による生成物。手で直接編集しない。
  データを直したいときは、DBを直すかスクリプトを直して再生成すること。

## 見た目・実装

- UIの構築には`frontend-design`スキルを使うこと。
- タイトル画面等のビジュアル素材はGPTで生成する方針。Claudeの画像生成機能は使わない。

## 公開前の必須事項

- `DISCLAIMER.md`の免責事項・著作権表記を、公開ページのフッター等に必ず反映すること。
- 特にPMDAデータの著作権表記（公共データ利用規約 第1.0版）は、公開前に
  「公共データ利用規約に関する重要情報」ページの最新記載を確認してから文言を確定すること。

## データ更新パイプライン

- `.github/workflows/update-drug-db.yml`が月次でDB更新・JSON再生成・デプロイまでを自動実行する。
- ワークフロー内の「PMDA更新チェック」ステップ（`scripts/check_pmda_updates.py`）は
  未実装のプレースホルダー。実装時はPMDAの検索ページの実際のリクエスト形式を
  実機で確認すること（公式ダウンロードAPIは存在しない）。
- このジョブが失敗した場合に気づけるよう、GitHub通知設定を必ず有効にしておくこと。

## 詳細設計

設計の経緯・UI仕様・データ構造の詳細は `HANDOFF.md` を参照。
