# CLAUDE.md

このリポジトリで作業するときは、以下を必ず守ってください。

## データの唯一の原本

- `karteno_drugs.db`（`pmda_tenpu_parser.py`が生成するSQLite）が全データの原本。
- `site/data/`配下のJSON類は`export_db_to_json.py`による生成物。手で直接編集しない。
  データを直したいときは、DBを直すかスクリプトを直して再生成すること。

## 見た目・実装

- UIの構築には`frontend-design`スキルを使うこと。
- 新規UI設計・実装・レビューは、必ず`DESIGN.md`（デザイン定義書 v1.0）の内容に従うこと。
  カラー・タイポグラフィ・アイコン方針・レイアウト・コピーライティング・
  「AIが陥りがちな手抜きパターン（禁止リスト）」まで規定している。Notion正典
  「看護薬理ノート 正典 v0.1」末尾の同名ドキュメントと同内容であり、Notion側が正。
  食い違いが見つかった場合はNotion側に合わせて`DESIGN.md`を更新すること。
- 新規UI設計時はClaude Codeの`/design`スキル（複数案生成）を活用し、1案だけで確定しない。
- タイトル画面等のビジュアル素材はGPTで生成する方針。Claudeの画像生成機能は使わない。

## 公開前の必須事項

- `DISCLAIMER.md`の免責事項・著作権表記を、公開ページのフッター等に必ず反映すること。
- 特にPMDAデータの著作権表記（公共データ利用規約 第1.0版）は、公開前に
  「公共データ利用規約に関する重要情報」ページの最新記載を確認してから文言を確定すること。

## データ更新パイプライン

- 月次更新は完全自動化しない（2026-09-12確定）。PMDAは添付文書の公式ダウンロードAPIを
  提供しておらず、検索ページの挙動に依存した自動スクレイピングは壊れやすく保守コストが
  高いため、自動差分検知は行わない方針にした。GitHub Actions
  （`.github/workflows/update-drug-db.yml`）の役割は**月次リマインドIssueの作成だけ**に
  限定し、DB更新・JSON書き出し・デプロイはクラウド側では一切行わない。
  - 毎月1日（cron）または手動（workflow_dispatch）：PMDAには一切アクセスせず、
    「今月の一括ダウンロードをお願いします」というGitHub Issueを作成するだけ
  - 人がPMDAから新記載要領XMLを一括ダウンロードし、`scripts/tenpu_xml/`に配置した上で、
    ローカルで`pmda_tenpu_parser.py`→`export_db_to_json.py`を実行する
    （`karteno_drugs.db`は引き続きローカルのみに置き、`.gitignore`のまま）
  - `site/data`配下の変更をコミット＆pushする。Netlify側の既存の自動デプロイ
    （gitプッシュ連携）がそのままサイトへ反映するため、GitHub Actions側に
    デプロイ用のステップは持たせない
- `scripts/check_pmda_updates.py`は上記方針に伴い**意図的に未使用**（未実装ではない）。
  ワークフローからは呼び出していない。詳細は同ファイルのdocstring参照。
- 月次リマインドIssueの見落とし、およびこのジョブが失敗した場合に気づけるよう、
  GitHub通知設定を必ず有効にしておくこと。

## 詳細設計

設計の経緯・UI仕様・データ構造の詳細は `HANDOFF.md` を参照。
見た目・デザインルールの詳細は `DESIGN.md` を参照。
